<script lang="ts">
  // Minimal page — the app runs as a background service with tray icon.
  // No GUI needed; all headphone control happens via RFCOMM protocol in the Rust backend.
</script>

<main style="display:none"></main>
